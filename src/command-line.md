commands used to unpack the alignments 

```
eti installed -i path/to/dros-62-install
```

and
```
eti alignments -i dros-62-install --align_name "dros-46-way" --outdir delme --ref dmel --mask CDS --limit 20
```
custom whole genome alignment hack, so check a few are reasonable
